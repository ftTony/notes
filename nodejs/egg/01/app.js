module.exports = app => {
    // app.cache = new Cache();
    // app.once('server', server => {

    // });
    // app.on('error', (err, ctx) => {

    // });

    // app.on('request', ctx => {

    // });

    // app.on('response', ctx => {
    //     const used = Date.now() - ctx.starttime;
    // });

}