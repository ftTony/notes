import {
    biggerEquals,
    Compare,
    defaultCompare,
    defaultEquals,
    defaultDiff,
    DOES_NOT_EXIST,
    lesserEquals
} from '../util'

export function interpolationSearch(array, value) {

}