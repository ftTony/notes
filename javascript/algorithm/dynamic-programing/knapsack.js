function findValues(n, capacity, kS) {
    let i = n;
    let k = capacity;
    while (i > 0 && k > 0) {
        if (kS[i][k] !== kS[i - 1][k]) {

        }
    }
}

export function knapSack(capacity, weights, values, n) {
    const kS = []
}